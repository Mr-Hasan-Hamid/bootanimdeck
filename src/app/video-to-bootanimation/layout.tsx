import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free Video to Android Bootanimation.zip Converter | Client-Side Compiler",
  description: "Convert MP4/WebM videos to uncompressed Android bootanimation.zip files completely client-side. Adjust trim range timeline, crop-to-fit screen resolutions, set loop behavior, and downsample quality scales for optimal boot speed.",
  keywords: [
    "video to bootanimation",
    "convert video to android boot animation",
    "MP4 to bootanimation.zip",
    "bootanimation.zip creator",
    "Android custom boot animation",
    "uncompressed STORE mode zip",
    "free boot animation converter online",
    "root android customization"
  ],
  openGraph: {
    title: "Video to Android Bootanimation.zip Converter | Online Compiler",
    description: "Build custom Android boot animation sequence files from your videos inside your browser. No server uploads, 100% private client-side converter.",
    type: "website",
  }
};

export default function VideoToZipLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
